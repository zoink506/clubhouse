<h1>Post Creation Form</h1>

<%= form_with model: @post do |f| %>
  <div>
    <%= f.label(:title) %>
    <%= f.text_field(:title) %>
  </div>

  <div>
    <%= f.label(:body) %>
    <%= f.text_field(:body) %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>